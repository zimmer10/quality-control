# Разметка LoRA

Планируемые файлы:

```text
annotations/lora_v1.jsonl
annotations/hard_examples.parquet
annotations/lora_v2.jsonl
```

Для каждого evidence сохраняются источник и уровень качества: `gold`, `silver_high`, `silver` или `label_only`.

