# Тесты

В первую очередь необходимо покрыть:

- расчёт competition metric;
- отсутствие пересечения `group_id` между folds;
- стабильность схем OOF-таблиц;
- отрицания и комплектность в rule engine;
- fold-safe retrieval;
- routing masks;
- формат `id,result` и длину объяснения.

